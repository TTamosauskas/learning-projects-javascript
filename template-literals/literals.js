const usuario = 'Bob';
const idade = 23;

//Modo antigo por concatenação

console.log('O usuário ' + usuario + ' possui ' + idade + ' anos');

// Modo novo usando template literals

console.log(`O usuário ${usuario} possui ${idade} anos`);

//Observer:
//1. Uso de crase ` em vez de aspas
//2. Uso de ${variavel} em vez de concatenação